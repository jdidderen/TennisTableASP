@model IEnumerable<TennisTableASP.Models.Sexes>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Sexes";
}

<p>
    @Html.ActionLink("Créer un sexe", "Create", null, htmlAttributes: new { @class = "btn btn-succes btn-default" })
</p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Denomination)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Denomination)
            </td>
            <td>
                @Html.ActionLink("Éditer", "Edit", new { id = item.SexeId }, htmlAttributes: new { @class = "btn btn-succes btn-small" })
                @Html.ActionLink("Supprimer", "Delete", new { id = item.SexeId }, htmlAttributes: new { @class = "btn btn-succes btn-small" })
            </td>
        </tr>
    }
    
    </table>